<div class="text-center m-5 pb-5">
<h1>Auteur n°<%=params['id']%></h1>

<h2>Prénom : <%= User.find(params['id']).first_name %> </h2>
<h3>Nom : <%= User.find(params['id']).last_name %> </h3>
<h4>Description : <%= User.find(params['id']).description %> </h4>
<h6>Email : <%= User.find(params['id']).email %> </h6>
<h6>Age : <%= User.find(params['id']).age %> </h6>
<h6>Ville : <%= User.find(params['id']).city.name %> </h6>
</h2>
<center><a href="<%= root_path %>" class="mt-3 mb-4 btn btn-lg btn btn-outline-info">retour aux ragots !</a></center>
</div>